<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  PasswordHistoryMapper SQL 정의.
  SPEC-CMS-002 REQ-AUTH-010 — 직전 5개 비밀번호 재사용 금지
-->
<mapper namespace="kr.co.ircp.cms.domain.auth.repository.PasswordHistoryMapper">

    <!-- 비밀번호 이력 삽입 -->
    <insert id="insert">
        INSERT INTO password_history (user_id, password_hash, changed_at)
        VALUES (#{userId}, #{passwordHash}, #{changedAt})
    </insert>

    <!-- 최근 N개 비밀번호 해시 조회 (최신 순) -->
    <select id="findRecentHashes" resultType="string">
        SELECT password_hash
        FROM password_history
        WHERE user_id = #{userId}
        ORDER BY changed_at DESC
        LIMIT #{limit}
    </select>

</mapper>
