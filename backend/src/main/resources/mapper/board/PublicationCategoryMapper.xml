<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    발간자료 카테고리 매퍼 XML.
    REQ-BOARD-012-D: 발간자료 카테고리 트리 조회
-->
<mapper namespace="kr.co.ircp.cms.domain.board.repository.PublicationCategoryMapper">

    <resultMap id="publicationCategoryResultMap" type="kr.co.ircp.cms.domain.board.entity.PublicationCategory">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="depth" column="depth"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- REQ-BOARD-012-D: 활성 카테고리 전체 조회 -->
    <select id="findAllActive" resultMap="publicationCategoryResultMap">
        SELECT id, code, name, parent_id, depth, sort_order, status, created_at
        FROM publication_category
        WHERE status = 'ACTIVE'
        ORDER BY sort_order, id
    </select>

    <!-- REQ-BOARD-012-D: 단건 조회 -->
    <select id="findById" resultMap="publicationCategoryResultMap">
        SELECT id, code, name, parent_id, depth, sort_order, status, created_at
        FROM publication_category
        WHERE id = #{id}
    </select>

    <!-- ── 어드민 CRUD (SPEC-CMS-PUB-CAT-001) ─────────────────────────────── -->

    <!-- REQ-PCA-004: 어드민용 전체 조회 (INACTIVE 포함) -->
    <select id="findAllForAdmin" resultMap="publicationCategoryResultMap">
        SELECT id, code, name, parent_id, depth, sort_order, status, created_at
        FROM publication_category
        ORDER BY sort_order, id
    </select>

    <!-- REQ-PCA-001: code 중복 확인 -->
    <select id="existsByCode" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM publication_category
        WHERE code = #{code}
    </select>

    <!-- REQ-PCA-001: 카테고리 INSERT (depth = DB 트리거 자동 계산) -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO publication_category (code, name, parent_id, sort_order, status)
        VALUES (#{code}, #{name}, #{parentId}, #{sortOrder}, COALESCE(#{status}, 'ACTIVE'))
    </insert>

    <!-- REQ-PCA-002: 카테고리 UPDATE -->
    <update id="update">
        UPDATE publication_category
           SET name       = #{name},
               sort_order = #{sortOrder},
               status     = #{status}
         WHERE id = #{id}
    </update>

    <!-- REQ-PCA-003: 카테고리 DELETE -->
    <delete id="deleteById">
        DELETE FROM publication_category WHERE id = #{id}
    </delete>

    <!-- REQ-PCA-003: 자식 카테고리 수 -->
    <select id="countChildren" resultType="long">
        SELECT COUNT(*) FROM publication_category WHERE parent_id = #{parentId}
    </select>

    <!-- REQ-PCA-003: 연결된 발간자료 수 -->
    <select id="countLinkedPublications" resultType="long">
        SELECT COUNT(*) FROM bbs_post_publication_meta WHERE publication_category_id = #{categoryId}
    </select>

</mapper>
