<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    FAQ 매퍼 XML.
    REQ-BOARD-007: FAQ 카테고리·정렬·검색
-->
<mapper namespace="kr.co.ircp.cms.domain.board.repository.FaqMapper">

    <resultMap id="faqResultMap" type="kr.co.ircp.cms.domain.board.entity.Faq">
        <id property="id" column="id"/>
        <result property="categoryCode" column="category_code"/>
        <result property="question" column="question"/>
        <result property="answerHtml" column="answer_html"/>
        <result property="answerText" column="answer_text"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="viewCount" column="view_count"/>
        <result property="status" column="status"/>
        <result property="metadata" column="metadata"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findByCategoryCode" resultMap="faqResultMap">
        SELECT * FROM faq
        WHERE category_code = #{categoryCode} AND deleted_at IS NULL AND status = 'PUBLISHED'
        ORDER BY sort_order, id
    </select>

    <select id="findAll" resultMap="faqResultMap">
        SELECT * FROM faq WHERE deleted_at IS NULL ORDER BY category_code, sort_order, id
    </select>

    <select id="findById" resultMap="faqResultMap">
        SELECT * FROM faq WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO faq (category_code, question, answer_html, answer_text,
            sort_order, status, metadata, created_by)
        VALUES (#{categoryCode}, #{question}, #{answerHtml}, #{answerText},
            #{sortOrder}, #{status}, #{metadata}::jsonb, #{createdBy})
    </insert>

    <update id="update">
        UPDATE faq SET
            category_code = #{categoryCode},
            question = #{question},
            answer_html = #{answerHtml},
            answer_text = #{answerText},
            sort_order = #{sortOrder},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id} AND deleted_at IS NULL
    </update>

    <update id="deleteById">
        UPDATE faq SET deleted_at = NOW(), updated_at = NOW()
        WHERE id = #{id} AND deleted_at IS NULL
    </update>

</mapper>
