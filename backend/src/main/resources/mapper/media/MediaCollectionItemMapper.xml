<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.ircp.cms.domain.media.mapper.MediaCollectionItemMapper">

    <resultMap id="itemResultMap" type="kr.co.ircp.cms.domain.media.entity.MediaCollectionItem">
        <result property="collectionId" column="collection_id"/>
        <result property="assetId" column="asset_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="addedAt" column="added_at"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO media_collection_item (collection_id, asset_id, sort_order)
        VALUES (#{collectionId}, #{assetId}, #{sortOrder})
        ON CONFLICT (collection_id, asset_id) DO NOTHING
    </insert>

    <select id="findByCollectionId" resultMap="itemResultMap">
        SELECT * FROM media_collection_item
        WHERE collection_id = #{collectionId}
        ORDER BY sort_order, added_at
    </select>

    <delete id="delete">
        DELETE FROM media_collection_item
        WHERE collection_id = #{collectionId} AND asset_id = #{assetId}
    </delete>

    <delete id="deleteByCollectionId">
        DELETE FROM media_collection_item WHERE collection_id = #{collectionId}
    </delete>

</mapper>
