<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    동의어 사전 매퍼 XML.
    SPEC-CMS-010 REQ-SEARCH-009: 동의어 CRUD + 활성 동의어 조회.
-->
<mapper namespace="kr.co.ircp.cms.domain.search.repository.SearchSynonymMapper">

    <resultMap id="synonymResultMap" type="kr.co.ircp.cms.domain.search.entity.SearchSynonym">
        <id property="id" column="id"/>
        <result property="term" column="term"/>
        <result property="synonym" column="synonym"/>
        <result property="locale" column="locale"/>
        <result property="status" column="status"/>
        <result property="description" column="description"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- REQ-SEARCH-009: 활성 동의어 조회 (검색 시 OR 확장용) -->
    <select id="findActiveByTerm" resultMap="synonymResultMap">
        SELECT * FROM search_synonym
         WHERE term = #{term}
           AND locale = #{locale}
           AND status = 'ACTIVE'
    </select>

    <!-- 운영자 CRUD 용 페이징 조회 (ACTIVE 한정) -->
    <select id="findAllActive" resultMap="synonymResultMap">
        SELECT * FROM search_synonym
         WHERE locale = #{locale}
           AND status = 'ACTIVE'
         ORDER BY term, synonym, id
         LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAllActive" resultType="long">
        SELECT COUNT(*) FROM search_synonym
         WHERE locale = #{locale} AND status = 'ACTIVE'
    </select>

    <select id="findById" resultMap="synonymResultMap">
        SELECT * FROM search_synonym WHERE id = #{id}
    </select>

    <!-- REQ-SEARCH-009: 동의어 등록 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO search_synonym (
            term, synonym, locale, status, description, created_by, updated_by
        ) VALUES (
            #{term}, #{synonym}, #{locale},
            <choose>
                <when test="status != null and status != ''">#{status}</when>
                <otherwise>'ACTIVE'</otherwise>
            </choose>,
            #{description}, #{createdBy}, #{createdBy}
        )
    </insert>

    <!-- REQ-SEARCH-009: 부분 갱신 (synonym, status) -->
    <update id="update">
        UPDATE search_synonym
           SET
        <if test="synonym != null and synonym != ''">
               synonym = #{synonym},
        </if>
        <if test="status != null and status != ''">
               status = #{status},
        </if>
               updated_by = #{updatedBy},
               updated_at = NOW()
         WHERE id = #{id}
    </update>

    <!-- REQ-SEARCH-009: soft delete (status=PAUSED) -->
    <update id="softDelete">
        UPDATE search_synonym
           SET status = 'PAUSED',
               updated_by = #{updatedBy},
               updated_at = NOW()
         WHERE id = #{id}
    </update>

    <select id="existsByTermAndSynonym" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM search_synonym
             WHERE term = #{term}
               AND synonym = #{synonym}
               AND locale = #{locale}
        )
    </select>

</mapper>
